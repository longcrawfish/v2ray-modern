当前 Profile: ws-tls

本地 YAML 文件：
${EXPORT_DIR}/clash.yaml

你可以将该文件通过静态文件服务、Nginx、对象存储或订阅接口暴露为 URL。

示例订阅地址：
${SUBSCRIPTION_SCHEME}://${SUBSCRIPTION_HOST}/sub/ws-tls/clash.yaml

推荐导入方式：
- Clash Verge Rev
- ClashX Meta
- Mihomo 系客户端

注意：
- 这是 YAML 订阅地址说明文件，不是自动生成的公网 URL
- 如需公网可访问，请将 clash.yaml 托管到你自己的服务器或订阅服务
